# `1` Ваш первый бот

Для начала вам нужно получить токен бота. Для этого откройте диалог с [Master Bot](https://max.ru/masterbot) и создайте нового бота, следуя инструкциям. После этого Master Bot отправит вам токен.

Создайте новый проект Dart и добавьте `max_bot_api`. Для этого откройте терминал и выполните следующие команды:
```sh
# Создайте новую папку и перейдите в неё
mkdir my-first-bot
cd my-first-bot

# Создайте новый Dart проект
dart create .

# Добавьте Max Bot API
dart pub add max_bot_api
```

Теперь создайте файл `bin/bot.dart`.

Создайте экземпляр класса `Bot` и передайте токен в его конструктор. Мы рекомендуем передавать токен через переменные окружения.

```dart
import 'dart:io';
import 'package:max_bot_api/max_bot_api.dart';

void main() async {
  // Создайте экземпляр класса `Bot` и передайте ему токен
  final bot = Bot(Platform.environment['BOT_TOKEN']!);

  // Теперь вы можете добавлять слушатели обновлений
  // Max Bot API будет вызывать их, когда пользователи взаимодействуют с ботом

  // Обработчик для команды '/start'
  bot.command('start', [(ctx, next) => ctx.reply('Добро пожаловать!')]);

  // Обработчик для любого другого сообщения
  bot.on(UpdateType.messageCreated, [
    (ctx, next) => ctx.reply('Новое сообщение'),
  ]);

  // Когда вы указали, как обрабатывать обновления, вы можете запустить бота
  // Он подключится к серверам Max и будет ждать обновлений

  await bot.start();
}
```

Чтобы запустить бота выполните следующие команды:
```sh
# Передайте переменную окружения и запустите бота
BOT_TOKEN="<your_token_here>" dart run bin/bot.dart
```

Поздравляем, вы написали первого бота! 🎉
